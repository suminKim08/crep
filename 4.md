### 4. 두 실수를 메모리에 저장하고 함수 Add()에서 더한 후 리턴한 값을 화면체 출력하는 C언어 프로그램을 작성하시오.
---
```
// 4. 두 실수를 메모리에 저장하고 함수 Add()에서 더한 후 리턴한 값을 화면체 출력하는 C언어 프로그램을 작성하시오.
#include <stdio.h>
double Add(double a, double b) {
  double c = a + b;
	return (c);
}
int main() { 
	double a = 1.1, b = 2.2;
	printf("%lf ", Add(a, b) );
}
```
```
3.300
```
